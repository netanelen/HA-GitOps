replicaCount: 1
image:
  repository: ste18nati/flask-aws-monitor # Change this
  tag: "v13" # Jenkins will update this tag
  pullPolicy: Always
resources:
  limits:
    cpu: "500m"
    memory: "128Mi"
  requests:
    cpu: "250m"
    memory: "64Mi"
service:
  type: LoadBalancer
  port: 5001
ingress:
  enabled: false
  # hosts:
  #   - host: chart-example.local
  #     paths: ["/"]
awsCredentials:
  accessKey: ""
  secretKey: ""
