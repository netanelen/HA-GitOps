replicaCount: 1
image:
  repository: ste18nati/flask-aws-monitor # Change this
  tag: "v12" # Jenkins will update this tag
  pullPolicy: Always
service:
  type: LoadBalancer
  port: 5001
ingress:
  enabled: false
  # hosts:
  #   - host: chart-example.local
  #     paths: ["/"]
awsCredentials:
  accessKey: ""
  secretKey: ""
