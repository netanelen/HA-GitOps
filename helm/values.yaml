replicaCount: 1
image:
  repository: netanele/flask-aws-monitor # Change this
  tag: "v11" # Jenkins will update this tag
  pullPolicy: IfNotPresent
service:
  type: LoadBalancer
  port: 5001
ingress:
  enabled: false
  # hosts:
  #   - host: chart-example.local
  #     paths: ["/"]
awsCredentials:
  accessKey: ""
  secretKey: ""
